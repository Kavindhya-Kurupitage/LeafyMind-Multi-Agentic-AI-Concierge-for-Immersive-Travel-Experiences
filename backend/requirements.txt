# =============================================================================
# LeafyMind Backend — Production Python dependencies
# Python 3.11+ | FastAPI | LangChain | PostgreSQL (asyncpg)
# =============================================================================

# --- Web framework ---
fastapi>=0.115.0,<1
uvicorn[standard]>=0.32.0,<1

# --- Database ---
sqlalchemy[asyncio]>=2.0.36,<3
asyncpg>=0.30.0,<1

# --- Configuration & validation ---
pydantic-settings>=2.6.0,<3
email-validator>=2.2.0,<3
python-dotenv>=1.0.0,<2

# --- Authentication ---
python-jose[cryptography]>=3.3.0,<4
passlib[bcrypt]>=1.7.4,<2
bcrypt>=4.0.1,<4.1.0

# --- HTTP & uploads ---
python-multipart>=0.0.9,<1
httpx==0.27.0

# --- LangChain & LLM ---
langchain>=0.3.0,<0.4
langchain-core>=0.3.0,<0.4
langchain-groq>=0.2.0,<0.3
langchain-openai>=0.2.0,<0.3
langchain-community>=0.3.0,<0.4

# --- Knowledge base (FAISS + local embeddings) ---
sentence-transformers>=2.2.0,<4
faiss-cpu>=1.8.0,<2

# --- Scheduling (feedback email jobs) ---
apscheduler==3.10.4

# --- Trip pack PDF generation ---
fpdf2>=2.7.0,<3
